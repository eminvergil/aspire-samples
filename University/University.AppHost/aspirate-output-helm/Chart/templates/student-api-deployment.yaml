apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: {}
  labels:
    app: student-api
  name: student-api
spec:
  minReadySeconds: 60
  replicas: 1
  selector:
    matchLabels:
      app: student-api
  strategy:
    type: Recreate
  template:
    metadata:
      annotations: {}
      labels:
        app: student-api
    spec:
      containers:
      - args: []
        envFrom:
        - configMapRef:
            name: student-api
        - secretRef:
            name: student-api
        image: '{{ .Values.images.studentapi }}'
        imagePullPolicy: IfNotPresent
        name: student-api
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 8443
          name: https
      terminationGracePeriodSeconds: 180